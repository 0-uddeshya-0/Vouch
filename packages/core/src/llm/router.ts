/**
 * Tiered LLM routing placeholder (Haiku / Sonnet escalation).
 * The worker constructs this for future integration; routing hooks are not wired yet.
 */
export class LLMRouter {
  // Reserved for tier-1 / tier-2 model selection and cost tracking
}
